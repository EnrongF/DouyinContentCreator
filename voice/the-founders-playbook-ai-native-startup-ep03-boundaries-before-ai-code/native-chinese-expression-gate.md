# Native Chinese Expression Gate

Slug: `the-founders-playbook-ai-native-startup-ep03-boundaries-before-ai-code`

## Gate Status

- Stage: `Native Chinese Expression Gate`
- Status: `Pass with fixes`
- Pause required: `yes`
- Prior gate: `voice/the-founders-playbook-ai-native-startup-ep03-boundaries-before-ai-code/script-draft.md`
- Next approved stage: `locked script / speech chunk plan`
- Not approved yet: `TTS`, `ASR`, `voice manifest`, `scene graph`, `render`

## Verdict

`Pass with fixes`.

The draft is clear and source-faithful. Required fixes:

| Draft line | Decision | Reason / Fix |
|---|---|---|
| `MVP 阶段` | Fix | TTS input should use `M V P 阶段`; subtitle can keep `MVP`. |
| `愿意推荐` | Pass | Good natural substitute for referral evidence. |
| `重新猜一遍产品边界` | Pass | Native and concrete; keep. |
| `稳定的产品和架构模型` | Fix | Slightly abstract. Use `产品边界和架构上下文` for precision. |
| `放到它能读到的地方` | Pass | Natural and source-faithful without over-naming `CLAUDE.md`. |

## Locked Expression Choices

- Spoken `MVP`: `M V P` in TTS input where needed.
- Spoken `AI`: `A I` in TTS input where needed.
- Avoid `CLAUDE.md` in narration; use visual/source note later if useful.
- Avoid `PMF` and Sean Ellis framing in narration.

## Revised Voice Line

Use this fix:

```text
这就是 AI 时代更隐蔽的技术债：代码可能能跑，但产品边界和架构上下文已经开始漂移。
```

## QA

Pass:
- Natural Mandarin口播.
- No stiff product tutorial tone.
- Source meaning preserved.
- English terms are limited and handled.

## Next Milestone

Lock `script.md`, then create `speech-chunk-plan.md` and `pronunciation.md`.
